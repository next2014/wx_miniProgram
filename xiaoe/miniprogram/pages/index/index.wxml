<view wx:for="{{array}}" wx:key="idx">
<van-card
  num="{{item.num}}"
  price="{{item.price}}"
  desc="{{item.desc}}"
  title="{{item.title}}"
  thumb="../../images/s32314546.jpg"/>
  <!-- 上边框 -->
  <view class="van-hairline--top" style="color:#fff"></view>
</view>